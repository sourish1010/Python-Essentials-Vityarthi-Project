# Python-Essentials-Evaluated-Course-Project
A lightweight Python-based inventory and billing system for small shops. It manages items (add/update/delete/search), generates bills with 18% GST, and automatically updates stock using a JSON file—no external dependencies required.
